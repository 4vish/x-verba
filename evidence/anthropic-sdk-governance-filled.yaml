# ═══════════════════════════════════════════════════════════════
# X-VERBA GOVERNANCE CONTRACT — FILLED EXAMPLE
# Anthropic Python SDK — anthropic-sdk-python
#
# This is what a governed version of the Anthropic SDK would
# look like. Every flagged location has been given a Pre-Node,
# Invariant, Threshold, Human Gate, and Terminal State.
#
# This is NOT the current state of the SDK.
# This is what Γ = 1.0 looks like for this codebase.
#
# Generated by Super Semantics — supersemantics.org
# ═══════════════════════════════════════════════════════════════

system_identity:
  identity_key: anthropic-sdk-python-d5112326
  system_name: Anthropic Python SDK
  version: "0.x"
  domain: ai-infrastructure
  governance_authority: Anthropic SDK Team
  context_profile: ai-app
  reviewed: true
  approved: false
  approved_by: null
  approved_date: null

scan_summary:
  files_scanned: 554
  ai_integrations_detected: 9
  findings:
    critical: 8
    high: 9
    medium: 75
  structural_gamma_proxy: 0.0
  governance_status: BELOW_THRESHOLD
  note: >
    This file shows what governance would look like if applied.
    Current SDK has no Pre-Nodes. This is the target state.

# ───────────────────────────────────────────────────────────────
# SECTION 3 — NODES
# Every AI call detected. Filled in with what each node does.
# ───────────────────────────────────────────────────────────────

nodes:
- AI-001:
    detected_at: src/anthropic/_client.py:384
    line_content: return AsyncAnthropicWithRawResponse(self)
    provider: anthropic
    description: >
      Returns an async Anthropic client wrapper that exposes raw HTTP
      responses. This is the entry point for every async AI call made
      through the SDK. Any application calling client.with_raw_response
      passes through this node.
    input_source: Application-level API key and request configuration
    output_destination: downstream processing — caller's async handler
    criticality: critical
    notes: >
      This node is the gateway to all async Anthropic API calls.
      Every governance gap in applications built on this SDK originates
      here. If this node fires without a Pre-Node, every downstream
      application inherits Gamma = 0.0.

- AI-002:
    detected_at: src/anthropic/_client.py:388
    line_content: return AsyncAnthropicWithStreamedResponse(self)
    provider: anthropic
    description: >
      Returns an async streaming client wrapper. Used for all streaming
      AI responses. Output is a continuous stream — once started,
      it cannot be interrupted without a circuit breaker.
    input_source: Application-level streaming request configuration
    output_destination: downstream streaming handler
    criticality: critical
    notes: >
      Streaming responses are harder to govern than single responses —
      the output forms incrementally. A Pre-Node is especially critical
      here because once the stream starts, intervention cost rises
      with every token.

- AI-003:
    detected_at: src/anthropic/lib/aws/_auth.py:24
    line_content: return boto3.Session(
    provider: aws_bedrock
    description: >
      Creates an AWS boto3 session for Bedrock authentication.
      This is the credential layer — it determines what AWS identity
      the SDK will use for all Bedrock AI calls.
    input_source: AWS environment credentials or explicit key/secret
    output_destination: Bedrock client authentication chain
    criticality: high
    notes: >
      Credential initialisation without validation means any
      misconfigured or compromised credential reaches Bedrock
      unchecked. An eligibility condition here prevents unauthorised
      Bedrock access.

- AI-004:
    detected_at: src/anthropic/lib/bedrock/_auth.py:24
    line_content: return boto3.Session(
    provider: aws_bedrock
    description: >
      Second boto3 session creation — Bedrock-specific auth path.
      Separate from AWS auth path. Both must be governed independently.
    input_source: Bedrock-specific AWS credentials
    output_destination: Bedrock API authentication
    criticality: high

- AI-005:
    detected_at: src/anthropic/lib/bedrock/_client.py:80
    line_content: session = boto3.Session()
    provider: aws_bedrock
    description: >
      Creates a boto3 session in the Bedrock client constructor.
      Fires at client instantiation — before any specific AI call.
      This is the earliest intervention point in the Bedrock path.
    input_source: Default AWS credential chain
    output_destination: Bedrock client instance
    criticality: high

- AI-006:
    detected_at: src/anthropic/lib/tools/_beta_runner.py:219
    line_content: response = self._client.beta.messages.create(
    provider: ai_framework
    description: >
      Synchronous beta messages call inside the tool runner.
      This is the AI call that executes tool use — the model
      decides which tools to call and with what arguments.
      Tool calls can trigger irreversible external actions.
    input_source: Tool definitions and conversation history
    output_destination: Tool execution pipeline
    criticality: critical
    notes: >
      Tool-use AI calls are the highest-risk category. The model's
      output directly triggers external system calls — APIs, databases,
      file systems. A Pre-Node here is mandatory.

- AI-007:
    detected_at: src/anthropic/lib/tools/_beta_runner.py:491
    line_content: response = await self._client.beta.messages.create(
    provider: ai_framework
    description: >
      Async version of the tool runner beta messages call.
      Same risk profile as AI-006 — tool-use AI call that can
      trigger irreversible external actions.
    input_source: Tool definitions and conversation history
    output_destination: Async tool execution pipeline
    criticality: critical

- AI-008:
    detected_at: src/anthropic/lib/tools/_beta_runner.py:267
    line_content: response = self.generate_tool_call_response()
    provider: ai_framework
    description: >
      Generates a synthetic tool call response for testing/mocking.
      Lower risk than AI-006/007 but still produces output that
      enters the tool execution pipeline.
    input_source: Internal tool call parameters
    output_destination: Tool execution pipeline
    criticality: medium

- AI-009:
    detected_at: src/anthropic/lib/tools/_beta_runner.py:539
    line_content: response = await self.generate_tool_call_response()
    provider: ai_framework
    description: >
      Async version of AI-008. Same risk profile.
    input_source: Internal tool call parameters
    output_destination: Async tool execution pipeline
    criticality: medium

# ───────────────────────────────────────────────────────────────
# SECTION 4 — PRE-NODES
# What must fire BEFORE each node. Currently ALL MISSING.
# This section shows what each Pre-Node should contain.
# ───────────────────────────────────────────────────────────────

pre_nodes:
- PN-AI-001:
    node_ref: AI-001
    location: src/anthropic/_client.py:384
    status: MISSING — must be added
    pre_node_detected_in_code: false

    conditions_that_must_be_true:
      - API key is present and has been validated against allowed key format
      - Request is within rate limit for this client instance
      - Caller has been authenticated at the application level
      - Request payload does not exceed maximum permitted token count

    conditions_that_block_execution:
      - API key is missing, malformed, or revoked
      - Rate limit exceeded for this client or application
      - Caller is not authenticated
      - Request payload exceeds token threshold

    what_happens_on_block: >
      Raise GovernanceError with the specific condition that failed.
      Log the blocked attempt with timestamp, caller identity, and
      failure reason. Do NOT expose API key status in the error message.

    input_sanitisation_required: true
    prompt_boundary_defined: true
    governance_log_on_entry: true

    code_example: |
      # What this Pre-Node looks like in code:
      def _pre_node_001(self, request_config):
          if not self.api_key:
              raise GovernanceError("PN-001: API key missing")
          if not self._rate_limiter.check():
              raise GovernanceError("PN-001: Rate limit exceeded")
          if not self._auth.is_authenticated():
              raise GovernanceError("PN-001: Not authenticated")
          self._governance_log.record("PN-001 passed", request_config)
          # Only now proceed to: return AsyncAnthropicWithRawResponse(self)

- PN-AI-002:
    node_ref: AI-002
    location: src/anthropic/_client.py:388
    status: MISSING — must be added
    pre_node_detected_in_code: false

    conditions_that_must_be_true:
      - All conditions from PN-AI-001 must pass
      - Streaming is explicitly permitted for this application context
      - A circuit breaker is configured to terminate runaway streams
      - Maximum stream duration is defined

    conditions_that_block_execution:
      - Streaming not permitted in current application context
      - No circuit breaker configured
      - No maximum stream duration defined

    what_happens_on_block: >
      Raise GovernanceError. Fall back to non-streaming response path
      if streaming is the only issue. Log with stream context.

    input_sanitisation_required: true
    prompt_boundary_defined: true
    governance_log_on_entry: true

    code_example: |
      # What this Pre-Node looks like in code:
      def _pre_node_002(self, stream_config):
          self._pre_node_001(stream_config)  # inherit base checks
          if not self._config.streaming_permitted:
              raise GovernanceError("PN-002: Streaming not permitted")
          if not self._circuit_breaker:
              raise GovernanceError("PN-002: No circuit breaker configured")
          self._governance_log.record("PN-002 passed", stream_config)
          # Only now proceed to: return AsyncAnthropicWithStreamedResponse(self)

- PN-AI-003:
    node_ref: AI-003
    location: src/anthropic/lib/aws/_auth.py:24
    status: MISSING — must be added
    pre_node_detected_in_code: false

    conditions_that_must_be_true:
      - AWS credentials are present in environment or explicit config
      - AWS region is explicitly configured (not defaulting)
      - Caller has IAM permission to invoke Bedrock
      - Credential source is from approved list (not arbitrary env vars)

    conditions_that_block_execution:
      - No AWS credentials found
      - Region not explicitly set
      - Credential source not in approved list

    what_happens_on_block: >
      Raise GovernanceError with credential validation failure.
      Do NOT log credential values. Log that validation failed
      and which condition was not met.

    input_sanitisation_required: false
    governance_log_on_entry: true

    code_example: |
      # What this Pre-Node looks like in code:
      def _pre_node_003(self, aws_config):
          if not aws_config.get('region'):
              raise GovernanceError("PN-003: AWS region not configured")
          if aws_config.get('credential_source') not in APPROVED_SOURCES:
              raise GovernanceError("PN-003: Credential source not approved")
          self._governance_log.record("PN-003 passed")
          # Only now proceed to: return boto3.Session(...)

- PN-AI-006:
    node_ref: AI-006
    location: src/anthropic/lib/tools/_beta_runner.py:219
    status: MISSING — must be added
    pre_node_detected_in_code: false

    conditions_that_must_be_true:
      - Tool definitions have been validated against approved tool list
      - No tool in the request can trigger irreversible actions without
        a separate human authorisation gate
      - Conversation history has been checked for contradiction injection
      - Maximum tool call depth has not been reached
      - Human authorisation has been obtained for any tool flagged
        as irreversible (database writes, external API calls, payments)

    conditions_that_block_execution:
      - Tool not in approved list
      - Irreversible tool requested without human authorisation
      - Maximum tool call depth exceeded
      - Contradiction detected in conversation history

    what_happens_on_block: >
      Raise GovernanceError with specific tool and condition.
      If irreversible tool requested, escalate to human gate before
      blocking. Log full tool call context for forensics.

    input_sanitisation_required: true
    prompt_boundary_defined: true
    governance_log_on_entry: true

    code_example: |
      # What this Pre-Node looks like in code:
      def _pre_node_006(self, tool_request):
          for tool in tool_request.tools:
              if tool.name not in self._approved_tools:
                  raise GovernanceError(f"PN-006: Tool {tool.name} not approved")
              if tool.is_irreversible and not self._human_gate.approved(tool):
                  self._human_gate.request_approval(tool)
                  raise GovernanceError("PN-006: Awaiting human approval")
          if self._call_depth >= self._max_depth:
              raise GovernanceError("PN-006: Max tool call depth exceeded")
          self._governance_log.record("PN-006 passed", tool_request)
          # Only now proceed to: response = self._client.beta.messages.create(

- PN-AI-007:
    node_ref: AI-007
    location: src/anthropic/lib/tools/_beta_runner.py:491
    status: MISSING — must be added
    pre_node_detected_in_code: false
    conditions_that_must_be_true:
      - All conditions from PN-AI-006 apply
      - Async context has not been cancelled
    conditions_that_block_execution:
      - Any condition from PN-AI-006
      - Async context cancelled
    what_happens_on_block: >
      Same as PN-AI-006. Async path requires same governance as sync path.
    governance_log_on_entry: true

# ───────────────────────────────────────────────────────────────
# SECTION 5 — INVARIANTS
# Rules that must ALWAYS hold. Cannot be bypassed. Ever.
# ───────────────────────────────────────────────────────────────

invariants:
- INV-001:
    rule: >
      No AI call may fire without a valid, non-expired API key that
      has been verified against the allowed key registry.
    scope: All nodes — AI-001 through AI-009
    cannot_be_bypassed: true
    violation_action: >
      Immediately terminate the request. Raise GovernanceError.
      Log the violation with caller identity and timestamp.
    severity: critical
    audit_on_check: true

- INV-002:
    rule: >
      No tool-use AI call may trigger an irreversible action
      (database write, external API call, payment, file deletion)
      without explicit human authorisation recorded in the audit trail.
    scope: AI-006, AI-007 — tool runner nodes
    cannot_be_bypassed: true
    violation_action: >
      Block the tool execution. Escalate to human gate.
      Record the attempted irreversible action in the governance ledger.
      Do not retry without human approval.
    severity: critical
    audit_on_check: true

- INV-003:
    rule: >
      Streaming responses must have a circuit breaker configured.
      A stream that has run for longer than max_stream_duration_seconds
      must be terminated automatically and logged.
    scope: AI-002, AI-007
    cannot_be_bypassed: true
    violation_action: >
      Terminate the stream. Log the duration and termination reason.
      Escalate if stream was terminated due to anomalous content.
    severity: high
    audit_on_check: true

- INV-004:
    rule: >
      AWS credential sources must be from an approved list.
      Arbitrary environment variable credential injection is not permitted.
    scope: AI-003, AI-004, AI-005
    cannot_be_bypassed: true
    violation_action: >
      Block session creation. Raise GovernanceError.
      Alert security team of unapproved credential source.
    severity: high
    audit_on_check: true

# ───────────────────────────────────────────────────────────────
# SECTION 6 — THRESHOLDS
# Quantitative limits that cannot be exceeded.
# ───────────────────────────────────────────────────────────────

thresholds:
- TH-001:
    node_ref: AI-001, AI-002
    parameter: max_tokens_per_request
    maximum_permitted: 8192
    on_breach: Block request and return GovernanceError
    rationale: >
      Unbounded token requests create unpredictable cost and output length.
      8192 is the recommended maximum for governed contexts.

- TH-002:
    node_ref: AI-006, AI-007
    parameter: max_tool_call_depth
    maximum_permitted: 5
    on_breach: >
      Terminate the tool call chain. Enter Terminal State TS-001.
      Require human authorisation to resume.
    rationale: >
      Recursive tool calls without a depth limit are the primary
      mechanism for DC-I6 Cascade Rupture. Knight Capital pattern.

- TH-003:
    node_ref: AI-002, AI-007
    parameter: max_stream_duration_seconds
    maximum_permitted: 300
    on_breach: >
      Terminate stream. Log. Escalate if content was anomalous.
    rationale: >
      Runaway streams with no time limit consume resources and
      may produce governance-violating content over extended duration.

- TH-004:
    node_ref: AI-001 through AI-009
    parameter: requests_per_minute
    maximum_permitted: 60
    on_breach: Block and return rate limit error with retry-after header
    rationale: Rate limiting is the primary defence against DC-E1 Threshold Assault.

# ───────────────────────────────────────────────────────────────
# SECTION 7 — HUMAN AUTHORISATION GATES
# Where automation must pause and a human must approve.
# ───────────────────────────────────────────────────────────────

human_authorisation_gates:
- HG-001:
    location: src/anthropic/lib/tools/_beta_runner.py:219
    ai_integration_ref: AI-006
    status: MISSING — must be added
    trigger_condition: >
      Any tool call that would execute an irreversible action —
      database write, external API POST/PUT/DELETE, payment,
      file deletion, email send, system command.
    authorisation_level: >
      Application owner or designated governance authority.
      Cannot be automated. Cannot be self-approved by the AI.
    what_human_must_review: >
      The full tool call parameters, the conversation context
      that led to this tool call, and the expected outcome.
    what_human_must_approve: >
      That this specific tool call with these specific parameters
      is intended, appropriate, and within governance policy.
    timeout_seconds: 300
    on_timeout: >
      Block the tool call. Log the timeout. Notify governance authority.
      Do not retry automatically.
    audit_trail_required: true

- HG-002:
    location: src/anthropic/lib/tools/_beta_runner.py:491
    ai_integration_ref: AI-007
    status: MISSING — must be added
    trigger_condition: Same as HG-001 — async path
    authorisation_level: Same as HG-001
    timeout_seconds: 300
    on_timeout: Block. Log. Notify. Do not retry.
    audit_trail_required: true

# ───────────────────────────────────────────────────────────────
# SECTION 8 — TERMINAL STATES
# Where automation stops completely. Human must authorise exit.
# ───────────────────────────────────────────────────────────────

terminal_states:
- TS-001:
    entry_conditions:
      - Tool call depth exceeds TH-002 maximum (5 recursive calls)
      - Irreversible action attempted without HG-001/HG-002 authorisation
      - INV-001 violated — API call attempted without valid key
      - INV-004 violated — unapproved credential source detected
    automated_transitions: NONE — by definition
    human_authorised_exit_required: true
    governance_authority: Anthropic SDK Team / Application Owner
    escalation_path: >
      1. Immediate notification to governance authority
      2. Full context logged to governance ledger
      3. No automated retry
      4. Human reviews context, approves or rejects resumption
      5. If approved, new governance instance created with fresh Identity Key
    response_time_sla: 30 minutes for critical, 4 hours for high
    notification_targets:
      - governance_authority
      - security_team (if INV-004 triggered)

- TS-002:
    entry_conditions:
      - Stream terminated due to anomalous content (TH-003 breach)
      - Content boundary violation detected in streaming output
    automated_transitions: NONE
    human_authorised_exit_required: true
    governance_authority: Application Owner
    escalation_path: >
      1. Stream content logged in full for review
      2. Notification to application owner
      3. Human reviews streamed content
      4. Approves resumption with modified parameters or rejects
    response_time_sla: 1 hour

# ───────────────────────────────────────────────────────────────
# SECTION 9 — DRIFT CLASS CANDIDATES
# Failure modes detected structurally. Confirmed below.
# ───────────────────────────────────────────────────────────────

drift_class_candidates:
- DC-FINDING-001:
    code: DC-I11
    name: Evaluative Decoupling
    location: src/anthropic/_client.py:384
    confirmed: true
    plain_english: >
      The SDK has no mechanism to detect when its output diverges
      from the governance objective. It cannot know when it is failing.
      Every application built on it inherits this blindness.
    invariant_ref: INV-001
    terminal_state_ref: TS-001

- DC-FINDING-002:
    code: DC-I11
    name: Evaluative Decoupling
    location: src/anthropic/_client.py:388
    confirmed: true
    plain_english: >
      Streaming path has the same decoupling as the standard path.
      Additionally, streaming responses are harder to evaluate — the
      output forms token by token with no natural checkpoint.
    invariant_ref: INV-001, INV-003
    terminal_state_ref: TS-001, TS-002

- DC-FINDING-003:
    code: DC-I11
    name: Evaluative Decoupling
    location: src/anthropic/lib/tools/_beta_runner.py:219
    confirmed: true
    plain_english: >
      Tool-use AI calls with no Pre-Node are the highest-risk pattern.
      The model decides what external actions to take. Without a
      Pre-Node, those decisions execute without any structural check.
      This is the Knight Capital failure mode in an AI context.
    invariant_ref: INV-002
    terminal_state_ref: TS-001

# ───────────────────────────────────────────────────────────────
# SECTION 10 — GOVERNANCE SCORE (TARGET)
# ───────────────────────────────────────────────────────────────

structural_gamma:
  current_proxy_value: 0.0
  target_value: 1.0
  threshold: 0.9
  current_status: BELOW_THRESHOLD
  target_status: ABOVE_THRESHOLD
  what_gets_us_to_1_0: >
    Add Pre-Nodes PN-AI-001 through PN-AI-009 before each node.
    Implement Invariants INV-001 through INV-004.
    Add Human Gates HG-001 and HG-002 to tool runner.
    Configure Terminal States TS-001 and TS-002.
    Every decision point then has a structural checkpoint.
    Gamma moves from 0.0 to 1.0.

# ───────────────────────────────────────────────────────────────
# NEXT STEPS — FOR ANTHROPIC SDK TEAM
# ───────────────────────────────────────────────────────────────

next_steps:
  step_1:
    action: Add _pre_node_001() before line 384 in _client.py
    effort: 2 hours
    gamma_impact: +0.11 (1 of 9 nodes governed)

  step_2:
    action: Add _pre_node_002() before line 388 in _client.py
    effort: 1 hour (inherits from step 1)
    gamma_impact: +0.11

  step_3:
    action: Add _pre_node_006() before line 219 in _beta_runner.py
    effort: 4 hours (requires human gate integration)
    gamma_impact: +0.11
    priority: HIGHEST — tool-use calls are highest risk

  step_4:
    action: Implement INV-001 and INV-002 as SDK-level enforcement
    effort: 1 day
    gamma_impact: Structural — applies across all nodes

  step_5:
    action: Add HG-001 and HG-002 to tool runner
    effort: 2 days (requires async human approval mechanism)
    gamma_impact: Closes the irreversible action gap entirely

  total_estimated_effort: 3-4 days for a senior engineer
  gamma_after_completion: 1.0
  note: >
    This is what x-verba compile will validate and x-verba qa
    will enforce on every commit. Coming in v0.2.

# ═══════════════════════════════════════════════════════════════
# END OF GOVERNANCE CONTRACT
#
# Generated by X-Verba — github.com/4vish/x-verba
# Super Semantics — Governance proven, not claimed.
# supersemantics.org
# ═══════════════════════════════════════════════════════════════
